<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Android App Links (Feb 2026)

              autoVerify="true" tells Android to fetch
              https://parasreward.com/.well-known/assetlinks.json and
              https://bugzappers.emergent.host/.well-known/assetlinks.json
              on first install. If the SHA256 fingerprint in those files
              matches the signing key used for this app, links to those
              domains will open the app directly instead of a browser
              chooser dialog.

              Path filter `pathPrefix="/"` makes EVERY page on those domains
              a candidate app link. We rely on the React Router inside the
              WebView (single-page-app) to resolve the actual route,
              including ?ref=ABC123 referral params, which then flow into
              localStorage via RegisterSimple's existing capture logic.

              To verify after install:
                adb shell pm get-app-links com.parasreward.prc
                adb shell pm verify-app-links (re-verify flag) com.parasreward.prc
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="parasreward.com" />
                <data android:host="bugzappers.emergent.host" />
                <data android:pathPrefix="/" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Google AdMob App ID (Paras Reward) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3556805218952480~1933993140"/>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <!-- Required for @capacitor/local-notifications on Android 13+ (API 33+).
         Without this, requestPermissions() silently no-ops and notifications
         (mining session reminders, etc.) never reach Android 13+ users. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
